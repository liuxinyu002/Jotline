[workspace]
resolver = "2"
members = ["src-tauri/crates/contracts", "src-tauri/crates/server"]

# server = 主进程 lib + bin（axum 领域服务；bin 为独立调试入口）；
# tauri 壳（主进程挂载点）Phase-11 加入，维持 src-tauri/crates/ 子目录约定
